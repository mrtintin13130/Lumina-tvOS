---
phase: 08-backend-contract-reconciliation
plan: 02
type: execute
wave: 1
depends_on:
  - 08-01
files_modified:
  - docs/tvos-api-contract.md
  - docs/tvos-backend-contract-tests.md
autonomous: true
user_setup: []
must_haves:
  truths:
    - Playback session, progress, watched, stream-token, HLS, and safe error payload semantics are documented for the proof loop.
    - AVKit-compatible stream-token transport is explicit for manifests, playlists, segments, and subtitles.
  artifacts:
    - docs/tvos-api-contract.md
    - docs/tvos-backend-contract-tests.md
  key_links:
    - CONT-07
    - CONT-08
    - CONT-09
status: complete
---

# Phase 8 Plan 02: Playback Payload And Safe Error Contract

## Objective

- **What:** Define the v1.1 proof-loop payload shapes and safe failure semantics.
- **Why:** The Swift client must align with real backend wire names and protected HLS behavior before hardware proof.
- **Output:** Updated contract and backend contract-test requirements for session/progress/token/HLS/error behavior.

## Context

@.planning/phases/08-backend-contract-reconciliation/08-CONTEXT.md
@docs/tvos-api-contract.md
@docs/tvos-backend-contract-tests.md
@rest-client/04-stream-playback.http
@lumina/LuminaCore.swift

## Tasks

<task type="docs">
  <name>Define playback payloads</name>
  <files>docs/tvos-api-contract.md</files>
  <action>Add session create/update/stop, movie progress read/write, watched, stream-token, and HLS payload rules using real snake_case wire names.</action>
  <verify>Document includes `media_type`, `media_id`, `position_seconds`, `duration_seconds`, `play_state`, `client_label`, and session ID semantics.</verify>
  <done>Phase 9 can update Codable DTOs from this section.</done>
</task>

<task type="docs">
  <name>Define token and error behavior</name>
  <files>docs/tvos-api-contract.md docs/tvos-backend-contract-tests.md</files>
  <action>Document query `stream_token` fallback for AVKit-derived HLS requests and safe error categories/redaction requirements.</action>
  <verify>Manifests, playlists, segments, and subtitles are all covered; error categories map to safe diagnostics.</verify>
  <done>Contract tests can assert protected playback and redaction behavior.</done>
</task>

## Verification

- Confirm CONT-07, CONT-08, and CONT-09 are satisfied by the docs.
- Confirm tokenized URLs are described abstractly, not with real token values.

## Success Criteria

- [ ] Payload names and semantics are wire-accurate.
- [ ] AVKit HLS token transport is explicit.
- [ ] Safe error and diagnostics behavior is testable.
