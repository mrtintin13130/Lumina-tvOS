---
phase: 08-backend-contract-reconciliation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/tvos-api-contract.md
autonomous: true
user_setup: []
must_haves:
  truths:
    - The tvOS contract document lists real v1.1 route keys for the movie proof path.
    - Current placeholder route assumptions are called out as Phase 9 alignment targets.
  artifacts:
    - docs/tvos-api-contract.md
  key_links:
    - CONT-05
    - CONT-06
status: complete
---

# Phase 8 Plan 01: Reconciled Route Matrix

## Objective

- **What:** Reconcile `rest-client/` endpoint examples, Phase 1 contract docs, capabilities fixtures, and current Swift assumptions into a v1.1 route matrix.
- **Why:** Phase 9 should update the Swift client without guessing route names, route keys, or compatibility gates.
- **Output:** Updated `docs/tvos-api-contract.md` with route keys, proof-path route matrix, and mismatch notes.

## Context

@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/08-backend-contract-reconciliation/08-CONTEXT.md
@rest-client/00-health-auth.http
@rest-client/04-stream-playback.http
@rest-client/05-catalog.http
@lumina/LuminaCore.swift
@luminaTests/luminaTests.swift

## Tasks

<task type="docs">
  <name>Define v1.1 route keys</name>
  <files>docs/tvos-api-contract.md</files>
  <action>Add a concrete route-key table for setup, auth, catalog movie proof, stream token, HLS, playback session, progress, watched, tracks, subtitles, diagnostics, watchlist, and favorites.</action>
  <verify>Every Phase 8 success-criteria route family has a stable key and concrete route or route family.</verify>
  <done>Phase 9 can build URL construction from documented keys.</done>
</task>

<task type="docs">
  <name>Reconcile endpoint inventory</name>
  <files>docs/tvos-api-contract.md</files>
  <action>Update the route matrix to match `rest-client/` examples and identify current placeholder Swift/test assumptions as Phase 9 replacement targets.</action>
  <verify>Movie browse/detail, HLS, stream-token, session, progress, and stop routes match `rest-client/` examples.</verify>
  <done>No proof-path route is vague or left as "equivalent" unless explicitly optional.</done>
</task>

## Verification

- Read the updated contract against CONT-05 and CONT-06.
- Confirm no secrets, token values, signed URLs, local paths, SQL details, stack traces, or raw subprocess output are introduced.

## Success Criteria

- [ ] Route keys are concrete and implementation-ready.
- [ ] Proof-path routes match `rest-client/`.
- [ ] Phase 9 mismatch targets are explicit.
