# 14-01 Summary

Implemented a local support summary on `AppModel` and updated Settings to show app build, server/API summary, validation state, signed-in user display name, diagnostics mode/event count, last safe error, and support ID.

The Settings surface remains local-only and remote-friendly, with the existing Revalidate and Sign Out actions preserved.

