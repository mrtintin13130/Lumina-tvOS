---
phase: 11-architecture-and-state-hardening
plan: 03
type: execute
wave: 3
depends_on:
  - 11-01
files_modified:
  - lumina/App/AppModel.swift
  - lumina/Playback/PlaybackStateModel.swift
  - lumina/Playback/AVKitPlayerView.swift
  - luminaTests/luminaTests.swift
autonomous: true
user_setup: []
must_haves:
  truths:
    - Playback proof loading, cancellation, progress reporting, session stop/update, AVKit failure diagnostics, and media-option diagnostics are separated from catalog/session mechanics.
    - Playback cancellation and sign-out/reset cannot leave stale playback proof or stale playback sessions.
    - Playback diagnostics remain redacted and support-safe.
  artifacts:
    - lumina/Playback/PlaybackStateModel.swift
  key_links:
    - ARCH-01
    - ARCH-02
    - ARCH-03
    - ARCH-04
status: complete
---

# Phase 11 Plan 03: Playback Lifecycle State Ownership

## Tasks

<task type="refactor">
  <name>Extract playback state model</name>
  <files>lumina/Playback/PlaybackStateModel.swift, lumina/App/AppModel.swift</files>
  <action>Create a focused `@MainActor` playback state owner around `PlaybackProofLoader`, current proof, playback load ID, progress reporting, session update/stop, AVKit failure recording, and media selection diagnostics.</action>
  <verify>`AppModel` delegates `loadPlaybackProof`, `playCatalogMovie`, `reportPlaybackProgress`, `exitPlayback`, `recordPlaybackFailure`, and media-option diagnostics through the extracted owner while keeping app phase transitions coherent.</verify>
  <done>Playback mechanics are understandable without reading catalog/search/detail code.</done>
</task>

<task type="stale-async">
  <name>Harden playback cancellation cleanup</name>
  <files>lumina/Playback/PlaybackStateModel.swift, luminaTests/luminaTests.swift</files>
  <action>Invalidate in-flight playback proof loads during exit, sign-out, and reset; stop created sessions when a late or failed proof load no longer owns the current playback request.</action>
  <verify>Tests cover cancellation/late-result cleanup and session stop behavior without leaking tokens or stale proof state.</verify>
  <done>Late playback work cannot move the app back into playback after reset/sign-out/cancel.</done>
</task>

<task type="integration">
  <name>Retarget AVKit player callbacks</name>
  <files>lumina/Playback/AVKitPlayerView.swift, lumina/App/AppModel.swift</files>
  <action>Route AVKit coordinator callbacks through the playback owner or a narrow app-facing playback callback surface instead of relying on broad `AppModel` mechanics.</action>
  <verify>Playback entry, failure recording, progress update, stop-on-exit, and media-option recording still work from the SwiftUI/AVKit bridge.</verify>
  <done>`AVKitPlayerView` depends on the narrowest practical playback API for Phase 11.</done>
</task>
