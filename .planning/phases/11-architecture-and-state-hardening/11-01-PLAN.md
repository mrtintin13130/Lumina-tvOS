---
phase: 11-architecture-and-state-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lumina/App/AppModel.swift
  - lumina/Auth/SessionStateModel.swift
  - luminaTests/luminaTests.swift
autonomous: true
user_setup: []
must_haves:
  truths:
    - Session, auth, saved-server validation, sign-in, sign-out, and reset ownership are separated from the central app coordinator.
    - Existing setup, server unavailable, sign-in, restore, and signed-in Home transition behavior remains intact.
    - Token and password failures continue to map to safe user-facing messages.
  artifacts:
    - lumina/Auth/SessionStateModel.swift
  key_links:
    - ARCH-01
    - ARCH-02
    - ARCH-04
status: complete
---

# Phase 11 Plan 01: Session/Auth State Ownership

## Tasks

<task type="refactor">
  <name>Extract session state model</name>
  <files>lumina/Auth/SessionStateModel.swift, lumina/App/AppModel.swift</files>
  <action>Create a small `@MainActor` session/auth state owner around `AuthSessionRepository`, `ServerSettingsStore`, `ServerConnectionTesting`, user/capability state, email/password fields, server URL, setup validation, restore, sign-in, sign-out, saved-server retry, server reset, and token access.</action>
  <verify>`AppModel` delegates session mechanics to the extracted type while preserving its SwiftUI-facing setup/sign-in methods and published values or adapter accessors.</verify>
  <done>Session/auth responsibilities no longer require reading catalog/playback code inside `AppModel`.</done>
</task>

<task type="test">
  <name>Preserve auth behavior tests</name>
  <files>luminaTests/luminaTests.swift</files>
  <action>Update or add focused tests for restore success, expired restore, sign-in success, token-store failure safe copy, sign-out cleanup, and reset-server cleanup against the extracted ownership boundary.</action>
  <verify>Tests prove password/token details are not exposed in user-visible state.</verify>
  <done>Existing auth/session tests still pass or are migrated to the extracted model with equivalent assertions.</done>
</task>
