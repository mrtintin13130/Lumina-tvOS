---
phase: 01-tv-api-contract-baseline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
user_setup: []
must_haves:
  truths:
    - Errors can be mapped into stable user-safe categories.
    - Diagnostics correlation excludes secrets and backend internals.
  artifacts:
    - docs/tvos-api-contract.md
    - luminaTests/Fixtures/error-envelope-stream-token-expired.json
    - luminaTests/Fixtures/error-envelope-validation.json
  key_links:
    - CONT-03
status: ready
---

# Phase 1 Plan 02: Define TV Error Envelope, Redaction, And Diagnostics

## Objective

- **What:** Define the error envelope, redaction rules, safe user categories, retryability, and diagnostics correlation fields.
- **Why:** The tvOS client must show actionable failures and collect support-useful context without leaking secrets.
- **Output:** Contract documentation plus representative error fixtures.

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-tv-api-contract-baseline/01-CONTEXT.md

## Tasks

<task type="docs">
  <name>Error envelope</name>
  <files>docs/tvos-api-contract.md</files>
  <action>Document stable error shape, client categories, retryability, correlation, and safe message mapping.</action>
  <verify>Envelope supports setup, auth, stream-token, HLS manifest/segment, unsupported track, missing media, and server restart failures.</verify>
  <done>Client can classify failures without exposing backend internals.</done>
</task>

<task type="docs">
  <name>Redaction policy</name>
  <files>docs/tvos-api-contract.md</files>
  <action>Document forbidden diagnostics data and allowed support correlation fields.</action>
  <verify>Forbidden list includes JWTs, stream tokens, passwords, Authorization headers, signed URLs, filesystem paths, SQL details, stack traces, and raw subprocess output.</verify>
  <done>Diagnostics can correlate playback sessions without monetization analytics or secret leakage.</done>
</task>

<task type="test">
  <name>Error fixtures</name>
  <files>luminaTests/Fixtures/error-envelope-stream-token-expired.json, luminaTests/Fixtures/error-envelope-validation.json</files>
  <action>Add safe representative JSON fixtures for Phase 2 error decoding tests.</action>
  <verify>Fixtures are valid JSON and contain safe messages only.</verify>
  <done>Client tests can assert category, retryability, and correlation decoding.</done>
</task>

## Verification

- `python3 -m json.tool luminaTests/Fixtures/error-envelope-stream-token-expired.json`
- `python3 -m json.tool luminaTests/Fixtures/error-envelope-validation.json`
- Manual review that error categories and redaction rules are documented.

## Success Criteria

- [ ] Error envelope is stable and client-classifiable.
- [ ] Diagnostics fields are useful and redacted.
- [ ] Fixtures represent validation and playback-token failures safely.
