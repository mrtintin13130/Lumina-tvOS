---
status: complete
slug: native-tvos-poster-card
date: 2026-06-17
---

# Quick Task Summary: Native tvOS Poster Card Buttons

## Completed

- Updated `CompactCatalogPosterButton` and `CatalogPosterButton` to use native SwiftUI `Button` labels with `.buttonStyle(.card)`.
- Removed poster-specific custom focus scale, shadow, animation, and focus stroke styling.
- Kept existing poster actions: compact/poster cards still open details, and episode poster cards still start playback.
- Moved poster title/subtitle/progress content outside the native card label so the focused card remains poster-sized.
- Kept rounded clipping on poster artwork and retained the existing focus callback for contextual Home updates.

## Verification

- `git diff --check`
- `xcodebuild -project lumina.xcodeproj -scheme lumina -destination generic/platform=tvOS -derivedDataPath ./.derivedData CODE_SIGNING_ALLOWED=NO build`
