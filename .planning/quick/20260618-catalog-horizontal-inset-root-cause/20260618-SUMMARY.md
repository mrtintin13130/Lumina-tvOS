---
status: complete
slug: catalog-horizontal-inset-root-cause
date: 2026-06-18
---

# Quick Task Summary: Catalog Horizontal Inset Root Cause

## Completed

- Confirmed the remaining spacing was caused by double horizontal insets: system/container safe area plus Lumina's explicit `TVLayout.safeHorizontalPadding`.
- Updated `HomeShellView` so the shared catalog `TabView` ignores horizontal safe area only, leaving top safe-area behavior intact for the native tvOS tab chrome.
- Updated `CatalogDetailPage` so foreground content ignores horizontal safe area only, leaving the detail backdrop full-bleed and keeping top/bottom behavior unchanged.
- Preserved Home's existing explicit 80 pt alignment and made Movies, TV Shows, Search, Settings, and details use the same single horizontal inset model.

## Verification

- `git diff --check`
- `xcodebuild -project lumina.xcodeproj -scheme lumina -destination generic/platform=tvOS -derivedDataPath ./.derivedData CODE_SIGNING_ALLOWED=NO build`
