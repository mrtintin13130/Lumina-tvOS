# Quick Task 20260606: Home Spotlight Landscape Rail

## Goal

Adjust Home section rendering so `spotlight_rail` sections remain horizontal shelves with landscape media cards instead of becoming full-width themed card stacks.

## Tasks

1. Split Home presentation routing.
   - Keep `genre_pills` as pills.
   - Route `spotlight_rail` to a horizontal landscape shelf.
   - Keep `cinematic_banner` and `editorial_grid` as full-width themed sections.

2. Add landscape shelf cards.
   - Use backdrop artwork with a 16:9 card shape.
   - Preserve tvOS focus scale, border, shadow, and detail navigation.

3. Verify.
   - Run generic tvOS build and test-bundle compile with signing disabled.
