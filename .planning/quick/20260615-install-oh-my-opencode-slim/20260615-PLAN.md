---
status: in_progress
date: 2026-06-15
task: install-oh-my-opencode-slim
---

# Quick Task: Install And Configure oh-my-opencode-slim

## Goal

Install `oh-my-opencode-slim` for the local OpenCode setup and verify the generated configuration.

## Plan

1. Confirm OpenCode is installed and inspect the existing user OpenCode config.
2. Run the non-interactive installer with bundled skills and background subagents enabled.
3. Verify generated plugin/config files and run the package doctor when available.
4. Record remaining human steps such as provider authentication and `ping all agents`.
