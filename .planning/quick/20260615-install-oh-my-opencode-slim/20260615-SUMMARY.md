---
status: complete
date: 2026-06-15
task: install-oh-my-opencode-slim
---

# Quick Task Summary: Install And Configure oh-my-opencode-slim

## Completed

- Installed Bun `1.3.14` into the active Node environment so the official `bunx` installer can run.
- Installed `oh-my-opencode-slim@2.0.2` for OpenCode.
- Enabled the `oh-my-opencode-slim` plugin in `/Users/martin/.config/opencode/opencode.jsonc`.
- Disabled default OpenCode agents and enabled OpenCode LSP integration.
- Enabled `OPENCODE_EXPERIMENTAL_BACKGROUND_SUBAGENTS=true` in `/Users/martin/.zshrc`.
- Generated `/Users/martin/.config/opencode/oh-my-opencode-slim.json` with the default `openai` preset active and `opencode-go` also available.
- Installed bundled OpenCode skills: `simplify`, `codemap`, `clonedeps`, `deepwork`, `reflect`, `oh-my-opencode-slim`, and `worktrees`.
- Ran `bunx oh-my-opencode-slim@latest doctor`; user config passed and the active preset is `openai`.

## Remaining Human Steps

- Run `opencode auth login` and authenticate the provider you want to use.
- Run `opencode models --refresh`.
- Restart the terminal or run `source /Users/martin/.zshrc` before launching OpenCode.
- Start `opencode` and run `ping all agents`.

## Notes

- OpenCode currently reports zero configured credentials, so agent verification cannot complete until provider authentication is done.
- The installer skipped the optional desktop companion in non-TTY mode. Re-run with `--companion=yes` if you want that companion installed.
